# 更新日志

## v0.0.46 (2026-09-23)

### 🔧 检查更新地址对齐

- 更新检查地址由旧仓库名 `ai-workbench` 改为现名 `lumi-workbench`，不再依赖 GitHub 的重定向，更新检查更稳。

## v0.0.45 (2026-09-23)

### ⚡ 备份去重（大幅省磁盘）

- 多份自动备份的正文文件改为**硬链接共享**：同一本书/笔记只要内容没变，10 份备份也只占一份磁盘。
- 内容一旦被修改，它对应的备份仍是修改前的独立副本，历史版本不受影响。
- 硬链接失败时（跨卷、文件系统不支持等）自动回退为普通复制，行为与原先一致、数据不受影响。
- 主数据 `workbench-data.json` 体积很小，仍保持独立复制，不与在用文件共享。

### 🐛 修复正文写入

- 笔记/图书正文改为**先写临时文件再原子重命名**，不再原地截断写入。避免写入过程中崩溃导致正文损坏，也是上面备份可以安全共享文件的前提。

## v0.0.44 (2026-09-14)

### ✨ 更新提示优化

- 启动时不再自动下载安装包，仅提示有新版本；更新需手动点击「去更新」。
- 更新提示只出现一次：同一版本关闭后再打开不会重复提示，等出了更新的版本才会再次提示。
- 更新提示带上 release 的更新内容，方便决定是否更新。

### 🐛 修复

- 修复图书详情页在图书被删除时可能因 hook 数量不一致导致的崩溃。

## v0.0.43 (2026-09-14)

### 🐛 修复重复任务删除问题

- 修复删除任务时误触发「完成」逻辑的问题：删除确认框的点击不再冒泡到整行，删除即真正删除。
- 删除重复任务（每天 / 每周 / 每月）时，连同同名、同重复类型、日期 ≥ 今天的所有后续副本一起删除，彻底停止继续生成。
- 完成重复任务生成下一次前先去重，避免同名副本重复堆积。

## v0.0.42 (2026-09-13)

### ✨ 健康管理重构

- 删除健康页「本周活力」面板（无用装饰），补剂 / 饮水 / 健康日程改为三列并排。
- 新增健康目标：步数、睡眠时长、饮水各有目标，卡片显示「连续达标 N 天」；目标在健康页「目标设置」弹窗编辑（即改即生效），不再占用偏好设置。
- 新增体检 / 长期指标：体重、血压、血糖，支持记录、查看历史、逐条删除。
- 补剂管理支持上移 / 下移排序；「今日补剂」超过 3 条出现内滚轮；剂量移到名称右侧。
- 健康日程关键词扩展（体检、就医、用药、理疗、心理、养生等）。

### ✨ 笔记导出

- 笔记支持导出为 Markdown / 纯文本 / JSON 到指定位置，并可打开所在文件夹。

### ✨ 补剂提醒

- 补剂到点发送系统通知（按每周哪几天 + 提醒时间）。

## v0.0.41 (2026-08-31)

### ✨ 检查更新

- 启动时自动检查新版本（GitHub Releases），发现新版会自动下载安装包并提示安装。
- 偏好设置新增「检查更新」入口，并显示当前版本号。

## v0.0.40 (2026-08-31)

### 🐛 托盘唤起修复

- 修复窗口缩进系统托盘后，通过快捷方式再次启动无法把窗口唤回前台的问题（单实例唤起改为：先置顶绕过 Windows 前台锁 → 抢焦点 → 延迟 300ms 取消置顶）。

## v0.0.39 (2026-08-24)

### ✨ 快捷键优化

- 图书 / 笔记 / 日记详情页支持按 Esc 返回列表（等同点击「返回」，新建未保存内容也会取消返回）。
- Esc 优先级：先关命令面板 / 弹窗 / 删除确认框，最后才是返回；输入框内按 Esc 不触发返回，避免误触。

### 🎨 图书页按钮调整

- 图书详情页右上角按钮重新排序：删除移到最左，返回移到最右。

## v0.0.38 (2026-08-24)

### ✨ 笔记板块优化

- 卡片 / 文件夹可拖拽移出：拖到面包屑「全部」回到未分类，拖到中间层级文件夹则移入该文件夹。
- 删除文件夹选「内容移回未分类」时，子文件夹保留并升级为顶级（内部笔记跟着走），不再连带删除。

### ✨ 数据备份优化

- 「备份数据」改为系统「另存为」对话框，默认定位到数据目录，保存后显示完整路径 + 「打开所在文件夹」按钮。

### 🚚 数据便携化

- 工作数据从系统 AppData 移到程序 exe 旁的 `data/` 目录（便携模式），整包拷贝即可迁移。
- 首次启动自动从旧 AppData 迁移数据（只拷贝、不删除）。

## v0.0.37 (2026-08-24)

### ✨ 新手引导

- 首次启动自动弹出聚光灯式新手引导，逐个介绍 10 个板块。
- 高亮侧栏对应板块按钮并自动切换页面，气泡说明具体功能。
- 支持上一步 / 跳过 / 完成，完成后不再自动弹出。
- 「偏好设置」内可重新打开新手引导。

## v0.0.36 (2026-08-24)

### 🐛 修复

- 修复安装包/应用图标显示为默认 Tauri 图标的问题，从月亮源图重新生成全套图标并配置 NSIS 安装包图标。
- 拖拽从 HTML5 原生拖拽改为**指针事件拖拽**，解决 WebView2 下无插入线、无法移入、位置偏差的问题。
- 改为**整张卡片拖动**，落点按左右分区：左半＝插到前面、右半＝插到后面、文件夹中央＝移入。
- 插入线改为卡片左右间隙的**虚线**提示。

### ✨ 笔记板块 UI 优化

- 面包屑与视图切换位置对调；「文件夹/全部笔记」更名为「笔记/展开」。
- 文件夹卡片布局调整：标题上移并放大，图标缩小并移到「N 个子文件夹 · N 篇笔记」右侧。
- 导入/导出按钮独立到右上角，与新建按钮分开。

### 🔧 开发环境

- vite 配置忽略 `src-tauri/target`、`data` 等目录，修复 `tauri dev` 在 Windows 下的 EBUSY 报错。

## v0.0.35 (2026-08-24)

### ✨ 笔记板块新增文件夹功能

- **多级嵌套文件夹**：点进文件夹查看内容（顶部面包屑返回上级），子文件夹同样平铺展示。
- **文件夹与笔记同列表平铺**：文件夹做成与笔记类似的卡片，一起平铺、一起拖拽排序。
- **拖拽归类**：拖右侧手柄，拖到卡片空隙＝排序，拖到文件夹卡正上方＝移入/嵌套（已做防环处理）。
- **文件夹自定义**：支持 4 色 + emoji 图标。
- **删除文件夹**：弹窗可选「内容移回未分类」或「连同内容删除」。
- **导入/导出按文件夹**：导入可进当前文件夹，导出支持按单个文件夹。
- **「全部笔记」视图**：一键跨文件夹平铺所有笔记。

## v0.0.34 (2026-08-24)

### 🚀 迁移到 Tauri（移除 Electron）

- 桌面框架从 Electron 迁移到 Tauri：安装包 **96.5MB → 3.3MB**，内存占用与冷启动大幅改善。
- 移除 Electron 相关代码（`electron/` 主进程、`electron-builder.yml` 及打包脚本）。
- 功能对齐：数据读写/正文拆分、托盘/最小化到托盘、单实例、开机自启、系统通知/定时提醒、节假日调休、自动备份/损坏恢复。
- 数据存储位置：`%APPDATA%\com.lumi.workbench\`（安装版，本地文件）。

### 🐛 修复

- 日记板块「最近记录」卡片底边与左侧心情日历对齐，多余日记在列表内部滚动。

## v0.0.33 (2026-08-23)

### ✨ 日记板块升级

- **最近记录可滚动**：从固定 3 条改为展示全部日记（按日期时间倒序），列表区内滚动查找更多历史。
- **搜索**：右上角放大镜展开搜索框，支持按**日期 / 标题 / 正文**匹配（不区分大小写），命中关键词在标题与日期上高亮。
- 右侧「最近记录」卡片底边自动与左侧心情日历对齐，随月份（5/6 行）伸缩。

### ⚡ 安装包轻量化

- 裁剪 Electron 语言包：55 种 → 仅 `zh-CN` + `en-US`，解压后体积 359MB → 314MB。
- NSIS 压缩级别提到 `maximum`，安装包 104.8MB → 96.5MB。

### 🔧 打包环境

- 新增 `.npmrc` 国内镜像（npmmirror），`electron-builder` 下载 Electron 二进制不再因直连 GitHub 超时。

## v0.0.32 (2026-08-22)

### ⚡ 打包方式改为安装版（NSIS）

- 从「便携版」换成「NSIS 安装版」：安装一次后，启动不再需要每次解压 94MB，启动速度大幅提升（消除便携版 7~20 秒的自解压开销）。
- 向导式安装，可自选安装目录；按用户安装，无需管理员权限。
- 数据仍存 exe 旁边的 `data/` 文件夹（现在是安装目录下的 `data/`）。

> ⚠️ 从便携版升级：请先用便携版「数据备份与恢复 → 备份数据」导出，再安装此版本后「恢复数据」导入，以迁移你的数据。

## v0.0.30 (2026-08-22)

### ✨ 新增

- **开机自启动**：应用支持随 Windows 开机自启，并可在「工作台偏好」面板中一键开关。便携版会正确指向你手里的 exe 注册登录项，而不是临时解压目录。

### 🚚 数据便携化

- 数据文件不再存系统盘 `%APPDATA%`，改为存放在 exe 旁边的 `data/` 文件夹中——exe 移到哪，数据就跟到哪。首次启动会自动把旧的 AppData 数据迁移到新位置，现有记录不会丢失。

### 🐛 修复

- **恢复备份不再覆盖偏好设置**：之前从备份文件「恢复数据」会把备份里的旧偏好（字体、主题、称呼等）一并覆盖，导致字体跳回「标准」。现在恢复时保留当前偏好，只恢复任务、账本、日记等记录数据。

### 🎨 默认偏好调整

- 全新安装的默认值调整为：称呼 `master`、标题 `master的工作台`、字体大小「大」、主题浅色。
